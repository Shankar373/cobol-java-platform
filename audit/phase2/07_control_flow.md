# Phase 2: Control Flow Graph (CFG) Design

The CFG tracks execution transitions:
`Program -> Paragraph -> Statement -> Control-flow edges`

It supports identification of branches, loops, PERFORM paths, CALL paths, and termination coordinates.
